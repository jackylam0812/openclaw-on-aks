apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: portal-db-pvc
  namespace: portals
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 1Gi
  storageClassName: managed-csi-premium
