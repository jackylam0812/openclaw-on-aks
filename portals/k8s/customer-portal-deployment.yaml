apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-portal
  namespace: portals
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customer-portal
  template:
    metadata:
      labels:
        app: customer-portal
    spec:
      containers:
        - name: customer-portal
          image: CUSTOMER_PORTAL_IMAGE
          ports:
            - containerPort: 3000
          env:
            - name: NEXT_PUBLIC_API_URL
              value: PORTAL_API_URL
          livenessProbe:
            httpGet:
              path: /app
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /app
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: customer-portal
  namespace: portals
spec:
  selector:
    app: customer-portal
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
